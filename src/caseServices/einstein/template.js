/* RCI-${externalId}: ${name}
${url}

Preconditions:${preconditions}
Entry point(/s):${entryPoint}
*/

describe('Please enter your describe....', () => {
  test({
    title: 'Please enter your title...',
    tags: [
      ['widgets'],
    ],
    levels: ['p${priority}'],
    options: [],
  }, async (context) => {
${children}
  });
});

